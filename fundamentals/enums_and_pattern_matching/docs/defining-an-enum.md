# Defining an Enum

Enums are ideal when a value can be one of a fixed set of variants.

Unlike many languages, Rust enums can store data inside each variant, including different data shapes per variant.
